# Suzuki Sales Website 維運

維護日期：2026-09-08。本文件是目前維運入口；日期報告保留當時檢查結果，不作為尚未修復問題的清單。SEO 改版已正式發布，Vercel 部署狀態及 GA4 設定已重新確認；帳務方案與商用資格依使用者指示延後確認。

## 部署
- **本次發布授權（2026-09-08）**：使用者已同意將本輪完成的 SEO 改版推送並上線，明確指示先略過 Vercel 商用資格確認，後續另授權代為設定 GA4。商用資格延後不代表已核實；未變更方案或帳務。
- **平台**: Vercel（既有紀錄為 Hobby，需確認目前方案）
- **自動部署**: GitHub push → Vercel auto-deploy
- **網域**: `suzuki-taipei.com` → Cloudflare DNS → Vercel SSL
- **優先確認商用資格**：2026-09-08 查閱 [Vercel 商用規則](https://vercel.com/docs/limits/fair-use-guidelines#commercial-usage)，Hobby 僅限非商業個人用途，宣傳商品／服務銷售被明列為商用。依本站購車諮詢用途判斷，若仍是 Hobby，應改用允許商用的方案或平台；本次未查看帳務、升級或產生費用。

## SEO
- [90 天 SEO 執行計畫](docs/SEO_PLAN_2026-09-08.md)：含公開網站檢查、關鍵字與頁面配置、車款獨立頁規格及成效量測。首頁定位、六款車頁、北投到店頁、四篇指南及事件程式已正式發布，GA4 已啟用並確認即時資料。量測基準與發布步驟見 [SEO 量測作業](docs/SEO_MEASUREMENT.md)；不保證排名。
- 2026-09-08 透過既有登入唯讀確認 Search Console：主要網址 `https://suzuki-taipei.com/` 已編入索引，Google 選定同一標準網址；sitemap 提交成功、探索到 1 頁。HTTP／www 三筆「頁面會重新導向」均為正常永久轉址排除，實測終點 200，無需取消轉址或要求替代網址收錄。詳見 SEO 計畫中的查核紀錄。
- 共用 layout 輸出 Website／顧問 Person／真實 AutoDealer／全台諮詢 Service；首頁 ItemList、各車款 Car／Offer、指南 Article 與麵包屑分頁輸出。每頁各自設定 metadata 與 canonical。
- 未經要求不改 metadata、sitemap 或公開聯絡入口。後續搜尋成效應以 Search Console 實際資料確認。

## Google Business Profile
- 原紀錄為待業務本人註冊，目前狀態待本人確認，不據此判定仍未註冊。
- 2026-09-08 核對 [Google 商家規範](https://support.google.com/business/answer/3038177?hl=zh-Hant)：企業業務人員不符合個人專業執業人員檔案資格；不應直接按舊筆記新建個人商家檔案。優先由授權管理者維護真實據點，網站連結是否適合代表該據點也需確認。

## 自動化
- Discord #交車照片 → 下載 → commit → push → Vercel deploy
- 以上為既有紀錄；本次未檢查或變更外部排程。
- 網站以 `/api/delivery` 讀取 `public/images/delivery-N.jpg`，編號缺號可以正常運作，不要為了連號刪除或重新命名。

## 諮詢收件

- 前端 → 同站 `/api/notify` → Vercel 私人 Blob 保存 → Discord 與 Google 試算表通知。先有持久收件才回傳成功與案件 UUID；相同內容重試沿用 UUID。
- 伺服器變數：`BLOB_READ_WRITE_TOKEN`、`DISCORD_WEBHOOK_URL`、`GOOGLE_SHEETS_WEBHOOK_URL`。`.env.example` 只列空值，真實值不得放入 Git 或文件。
- 私人儲存路徑：`consultations/<UUID>.json` 為收件；`delivery/<UUID>.json` 為通知狀態。
- `failed`、`unconfirmed`、`not_configured` 或缺少通知紀錄時，先按 UUID 核對接收端，再決定補送。未知結果不自動重送，避免重複通知；缺少通知不代表沒有收件。
- 既有部署紀錄記載 `/api/notify` 防火牆每 IP 每 10 分鐘 5 次；這是 Vercel 設定，不是程式內限流，也不保證其他環境受保護。
- 第一筆實際諮詢應人工核對 Blob、Discord 與試算表。主動全鏈路測試會產生外部資料及通知，需另行授權。
- 更正或刪除案件需同步處理 Blob、Discord 及試算表。保存期限、定期清理及待處理案件流程尚需業務決定。

## 內容與圖片維護

- 車款規格／售價：`src/data/site.ts`；優惠期間：`src/data/promotions.ts`。本次重新查看官方 9 月公告與六款起價，與現有資料一致；未重新逐頁驗證規配表，不改全站規格核對日期。
- [官方 9 月公告](https://www.taiwansuzuki.com.tw/news/606) 的現行方案至 2026/09/30。程式於台灣時間 10/01 00:00 隱藏到期優惠；每月仍須人工查核與替換活動、條件和來源。
- 圖片已於上一階段壓縮，車款彈窗／交車照片使用 Next Image；詳見 [素材紀錄](docs/IMAGE_ASSETS.md)。目前仍有原生 img 和外部字型，可在量測後再決定優化。
- `public/personal/dealer-portrait.jpg` 沒有在目前程式引用，但已有備用照片紀錄，本次保留。`favicon.svg` 與 `suzuki-logo.svg` 內容相同，但兩個公開路徑各有用途，保留。

## 本次整理（本機，尚未部署）

- 移除 Git 追蹤的舊首頁 `src/app/page.tsx.bak` 與 `tsconfig.tsbuildinfo`；前者含舊試算表接收網址。新增備份、快取及 Playwright 報告的忽略規則。Git 歷史仍保留舊內容，不代表舊接收端已停用。
- 新增 README、空值環境變數範本，更新 AGENTS.md，標記早期報告為歷史狀態。
- 保留三份有效回歸測試與 Playwright 驗證工具；沒有發現需要刪除的殘留測試報告。一般 `.next/`、`node_modules/`、`.vercel/` 為工具資料，未手動清理。
- 貸款服務入口改由資料中的 href 決定，名稱改為「貸款試算」，保險需求另行諮詢；移除未開通 IG 佔位。
- 交車照片讀取增加 HTTP／資料格式／取消與失敗處理，輪播邊界按鈕加入真正的 disabled 狀態。

## 後續優先事項

部署方案的商用資格優先確認，見上方「部署」；以下是程式與網站營運待辦。

1. **收件作業可靠性**：確認實際案件全鏈路、安排待處理案件核對方式與資料保存期限；既有 Apps Script 接收端仍需評估簽章或更換端點，網站防火牆無法保護它。勿僅因刪掉備份就視為完成。
2. **鍵盤操作（本機已改善）**：車款、比較及照片檢視器改用原生 modal dialog；補齊初始焦點、Tab／Shift+Tab 循環、Esc 關閉最上層與返回觸發按鈕。比較表可聚焦後以方向鍵捲動。[WAI 對話框規範](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/)
3. **諮詢入口**：「我有興趣」帶入車款、保留輸入後直達聯絡表單，焦點移到諮詢標題。後續依使用者要求移除各車款「試算月付」入口，貸款試算統一由首頁服務入口進入並選擇車款。
4. **成效資料**：GA4 已啟用；持續核對 LINE／電話意向、持久收件 `generate_lead` 與人工有效案件。不得把點擊視為成交；正式自然收件與通知仍須按既有私人紀錄核對。
5. **車款獨立頁與內容（已上線）**：六款車、到店頁及四篇指南已有獨立網址、metadata 與 sitemap。後續持續由顧問核對內容，觀察逐頁收錄與有效諮詢；不能保證排名。[Google SEO 入門](https://developers.google.com/search/docs/fundamentals/seo-starter-guide)
6. **試算精度與速度**：貸款試算可加入本金／頭期款金額輸入；效能則先量測手機實際載入，再決定首頁拆分、字型及剩餘圖片優化。

## 例行檢查

- 每月：優惠、官網規配表、照片來源及內容核對日期。
- 每次程式／依賴變更：`npm test`、`npm run build`、`git diff --check`；依賴更新另跑 `npm audit`。
- 每次視覺修改：桌面與手機瀏覽，測試必要的點擊、圖片讀取、捲動及表單攔截回應。
- 正式部署後：首頁／robots／sitemap、實際互動、收件及通知狀態；本機通過不能取代正式環境驗收。

## 前次整理驗證結果

- `npm test`：15 項通過；`npm run build` 與 `git diff --check` 通過。
- `npm audit`：完整依賴掃描 0 項已知漏洞。這不是完整安全審查或未來保證。
- 本機正式版本：1280 × 900、390 × 844、360 × 780 無水平溢出；服務區截圖檢視、貸款連結、輪播首末頁按鈕與表單模擬 503 保留輸入均通過，沒有 pageerror。
- 交車 API 模擬 HTTP 503、無效 JSON、錯誤資料結構、不合法圖片編號、網路失敗，均未產生未處理的例外。
- 表單請求由瀏覽器攔截，未送往真正收件端；未提交、推送或部署，未變更正式環境。截圖與一次性驗證程式未加入專案。

## 後續使用者調整（本機，尚未部署）

- 貸款試算預設與最低年利率為 3%，最高保留 8%，可輸入 3.19% 等小數；超出範圍依上下限計算，離開欄位時校正顯示。此為試算設定，未更動官方活動利率。
- 試算右側說明改為左對齊、加深文字、增加行距的三點清單，分開說明計算方式、範例利率及核貸條件。
- 上方服務據點顯示「Suzuki 北投所」；車款名稱顯示「Jimny 2026」，比較、試算、表單及由車款生成的 JSON-LD 同步。接收端保留舊 Jimny 名稱相容性。
- SWIFT 第二張改為官方完整後側行駛外觀，見圖片素材紀錄。
- 新增 `npm run test:browser` 與 `tests/browser/`，持續驗證動線、焦點與利率輸入；測試會啟動無真實接收憑證的本機正式版本。
- 本次驗證：16 項單元測試、桌面 1280 × 900／手機 360 × 780 共 8 項 Chromium 瀏覽器測試通過；正式建置與 `git diff --check` 通過。已檢視試算區、車款彈窗及 SWIFT 新照片截圖，未發送真實通知或部署。

## 車款彈窗精簡（後續本機調整，尚未部署）

- 移除各車款的「試算月付」及不再使用的帶入參數；首頁貸款試算與 3% 下限保留。
- 車名、簡介及照片頁數整合為 48px 高的單行淺色列；照片描述保留於替代文字、輔助工具播報及放大檢視器。
- 車色參考改為單行橫向捲動，支援觸控、觸控板及聚焦後的左右方向鍵／Home／End；桌面另提供左右箭頭，捲到邊界時停用對應按鈕，手機維持滑動。
- 圖片來源、規格核對與官方規配表收進「圖片與規格說明」；優惠細則及官方來源收進「優惠條件」。保留方案利率、須審核及有效期間於預設畫面，優惠資料與到期判定沿用原有規則。
- 驗證：16 項單元測試及桌面／手機共 12 項瀏覽器測試通過，含六款車的單行排版、原生觸控滑動、展開說明的鍵盤操作與諮詢動線；正式建置通過，已檢視桌面及手機截圖。

## SEO 改版本機驗收紀錄（2026-09-08，以下保留發布前時點）

- 首頁以台北為主要服務地區，說明全台購車諮詢與跨縣市購車／交車協助；北投為實際據點，交車地點、費用與時程依個案確認。
- 新增 `/cars/swift`、`/cars/jimny`、`/cars/e-vitara`、`/cars/vitara`、`/cars/s-cross`、`/cars/carry`、`/visit/beitou` 及四篇 `/guides/` 指南。首頁加真實詳情連結，保留彈窗、比較與貸款入口；sitemap 列 12 頁，日期來自重要內容更新紀錄。
- `src/data/content.ts` 維護各頁文案、關聯連結、更新與核對日期。既有主要規格核對仍為 2026-09-07；本次另核對官方起價、e VITARA 兩版本售價與北投所資訊，未把全部規配表日期改成今天。
- 表單及比較狀態放入共用 provider，站內切頁保留輸入、選取與請求 ID；`/?car=<既定ID>#contact` 帶入車款並聚焦諮詢標題。個資不寫瀏覽器持久儲存，硬重新整理不保留草稿。原收件／通知 API 未改。
- `src/lib/analytics.ts` 統一分析事件白名單及收件事件去重。`NEXT_PUBLIC_GA_MEASUREMENT_ID` 空值或非正式網域時不載入 GA4；正式設定與啟用需另行授權。`generate_lead` 只在持久收件回應與 ID 相符後送出，分析故障不影響收件。
- 已唯讀補取 GSC 台灣基準：2026/06/06–09/05 為 530 曝光／21 點擊；2026/08/09–09/05 為 188／6，前 28 天為 175／6。網頁表與資源總計有差異，原值及限制見 [SEO 量測作業](docs/SEO_MEASUREMENT.md)。完整可見查詢表保存在已忽略的 `.seo-private/`。
- 本次驗證：20 項單元測試、26 項桌面／手機 Chromium 測試、正式建置及 `git diff --check` 通過。12 頁 HTML／metadata／連結、404、跨頁草稿及請求 ID、原互動與模擬 GA4 去重均納入回歸；已檢視代表版面截圖。表單與分析外送均隔離，詳細紀錄及框架 404 日誌說明見 SEO 量測作業。
- 待正式發布前：顧問核對新指南及選車建議、核對 Vercel 商用資格，取得部署／正式 GA4 設定授權。未新增商家檔案、未改轉址、未推送、未部署、未提交索引或發送真實測試諮詢。

## SEO 正式發布及 GA4 啟用（2026-09-08）

- 使用者同意本輪內容上線，並授權 GA4 設定；商用資格依指示略過。程式提交為 `1355eb3754100eeb999b3d98d4c0481730a55229`，已進入 GitHub `main`。本機 GitHub CLI 憑證失效，改用既有 GitHub 連線建立完全相同的檔案樹，再同步本機分支；未 force push。
- GitHub 自動部署成功；新增正式 GA4 環境變數後，重新建置相同提交，部署 `dpl_FxXLWLmNQwDipoTr2BSTyho9UedH` 為 READY 並綁定正式網域。重新部署曾被自動核准要求核對版本；取得 Vercel API 的正式網域及相同 Git SHA 證據後通過。
- 正式 12 頁均為 200、各自唯一 canonical／H1；sitemap 共 12 頁，robots 正常。未知路徑回 404，HTTP／www 仍導向 HTTPS 裸網域。SWIFT 頁至表單可預選 SWIFT 並聚焦 `contact-heading`。
- 新建 GA4 資源 `suzuki-taipei.com`（ID `553150800`），台灣時區／新台幣；正式 Measurement ID `G-QX7R68FJE4`。加強型評估及非必要帳戶資料共用已關閉，`generate_lead` 為主要事件、每事件一次、無預設金額；已建立事件範圍 `car_id` 與 `entry` 維度。
- GA4 即時報表已收到本次驗收的 2 次 `page_view`（SWIFT／首頁各一次）與 1 次 `consultation_click`，沒有觸發 `generate_lead`。此為部署驗收流量，不列為客戶諮詢或 SEO 成效。原有諮詢後端、通知、防火牆及 DNS 設定未改。
- 正式收件成功事件及外部通知等待自然案件核對，未送出真實測試表單。新頁索引及第 30／60／90 天成效仍待累積；詳見 SEO 量測作業。
